name: r_popgen_env
channels:
  - conda-forge
  - bioconda

dependencies:
  # R core
  - r-base
  - r-remotes # access to github package downloads (pophelper)
  - gxx_linux-64 
  - gcc_linux-64

  # tidyverse ecosystem
  - r-tidyverse
  - r-data.table

  # popgen R packages
  - r-adegenet
  - r-vcfr
  - r-pegas
  - r-arrow
  - r-dartr

  # genomics tools (Linux)
  - plink
  - bcftools
  - htslib

  # genomics tools (Python)
  - python
  - cyvcf2
  - pyvcf
  - pysam

  # utilities
  - pandas
  - git
  - curl
  - pip